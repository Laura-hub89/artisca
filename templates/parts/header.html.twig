<header class="pacif">

	<div class=" d-flex justify-content-between">

		<div>
			<a class="navbar-brand" href="{{ path('home')}}">
			<img src="{{ asset('image/logo.PNG') }}" class="tlogo">
			</a>
		</div>

		<nav class="navbar navbar-expand-lg me-3 navbar-dark">
			<div class="container">
				<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
					<span class="navbar-toggler-icon"></span>
				</button>
				
				<div class="collapse navbar-collapse" id="navbarSupportedContent">
					<ul class="navbar-nav me-auto mb-2 mb-lg-0">
						<li class="nav-item">
						    <a class="nav-link" href="{{ path('atelier')}}">Ateliers</a>
						</li>
						<li class="nav-item">
							<a class="nav-link" href="{{ path('evenement')}}">Evénements</a>
						</li>
						<li class="nav-item">
							<a class="nav-link" href=" {{ path('nous')}} ">A propos</a>
						</li>
						<li class="nav-item">
							<a class="nav-link" href="{{ path('contact')}}">Contact</a>
						</li>
						<li class="nav-item">
							<a class="nav-link" href="{{path('app_register')}}">S'inscrire</a>
						</li>
					</ul>
					<ul class="nav navbar">
						{% if app.user  %}
						    <a class="nav-link dropdown-toggle" data-bs-toggle="dropdown" aria-expanded="false">Bonjour {{ app.user.prenom }} </a>
                            <ul class="dropdown-menu dropdown-menu-end" aria-labelledby="navbarDropdown">
						        <li class="nav-item">
						            <a class="nav-link active text-dark" href="{{path('moncompte')}}">Mon compte</a>
					            </li>
								<li class="nav-item">
								<a class="nav-link active text-dark">Reservation : {{app.user.reservations|length}}</a>
								</li>
							    <li class="nav-item">
								    <a class="nav-link active text-dark" href="{{path('app_logout')}}">Se déconnecter</a>
							    </li>
							</ul>  


						{% else %}
							<li class="nav-item">
								<a class="nav-link active" aria-current="page" href="{{path('app_login')}}"><i class="fas fa-user"></i></a>
							</li>
						{% endif %}
					</ul>
				</div>
			</div>
		</nav>
	</div>

</header>
