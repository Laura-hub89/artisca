{% extends 'base.html.twig' %}

{% block body %}
<div class="container">

<div class="text-center">
		<h1 class="text-center">Contactez-Nous !</h1>
		<div class="d-flex justify-content-center">
				<hr class="w-75">
			</div>
	</div>
	<div class="text-center">
		<h4>Vous avez un projet, une question sur l'un de nos ateliers !
		</h4>
	</div>

	<div class="intro fst-italic text-center w-75 text-center mx-auto">
		<p>N'ésitez pas à nous contacter sur nos différents réseaux ainsi que sur l'adresse mail suivante : association@artisca.fr !
			<br>
			Vous pouvez également utiliser le formulaire ci-dessous :
		</p>
	</div>

	<div class=" p-5 w-50 contact-form mx-auto">

		<div class="form-group ">
			{{ form_start(form) }}
			{{ form_row(form.nom, {'attr': {'class': 'form-control'}}) }}
		</div>
		<div class="form-group">
			{{ form_row(form.email, {'attr': {'class': 'form-control'}}) }}
		</div>
		<div class="form-group ">
			{{ form_row(form.message, {'attr': {'class': 'form-control'}}) }}
		</div>
		<div class="form-group text-center">
			<input type="submit" value="Envoyer" class="btn btn-outline-bleuet rounded mx-auto mt-2">
		</div>
		{{ form_end(form) }}
	</div>
</div>

</div>
	{% endblock %}
